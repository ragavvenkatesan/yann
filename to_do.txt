
1. Layerwise greedy unsupervised pre-training. { Probably an abandoned option because dropouts perform so well }
2. Parse code for visualizing filters possibly in matlab similar to the other thing .. with GUI options to load choice of epoch. 
3. Provide support for Caltech 256 (working), PASCAL VOC and ImageNet. 


