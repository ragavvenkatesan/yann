.. _network_module:

:mod:`network` - The network module 
===================================

The module :mod:`network` in ``yann.network.__init__.py`` contains the main network class that 
creates networks. It also contains other modules that the network might use. Below is a listing of 
all the members of the module. 
 
.. toctree::
   :maxdepth: 3
   :name: Network Module     

   network
   modules
   layers
